anthropic>=0.40
pyyaml>=6.0
fastapi>=0.110
uvicorn>=0.29
numpy>=1.26            # busca de similaridade do RAG (em memória)
# Driver do banco — instalar o do backend usado (DATABASE_URL):
psycopg[binary]>=3.1   # PostgreSQL (Cloud SQL)
PyMySQL>=1.1           # MySQL / MariaDB (RDS)
boto3>=1.34            # usado só quando EMBED_PROVIDER=bedrock (AWS Bedrock Titan)
